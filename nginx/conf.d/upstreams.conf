# Beyond The Club services
upstream btc-api {
    server btc-api:8000;
    keepalive 32;
}

upstream btc-web {
    server btc-web:3000;
    keepalive 32;
}

upstream btc-mcp {
    server btc-mcp:8001;
    keepalive 32;
}

# Voice Orchestrator
upstream voice-orchestrator {
    server voice-orchestrator:8000;
    keepalive 32;
}

# SmartThings OAuth
upstream smartthings-oauth {
    server smartthings-oauth:8000;
    keepalive 32;
}
