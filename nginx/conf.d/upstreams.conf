# Upstreams removed — using resolver 127.0.0.11 with variables in location blocks
# to avoid DNS resolution failures at nginx startup after VPS reboots.
# Each location block uses: set $backend "http://<container>:<port>"; proxy_pass $backend;
